name: Android CI

on:
  push:
    tags:
      - dev/**
      - release/**

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
      
    - name: Build with Gradle
      run: |
        javac -v

