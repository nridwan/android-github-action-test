name: Android CI

on:
  push:
    branches: 
      - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Test Search File
      run: |
        test=$(find . -iname '*.kt' | head -n 1)
        echo $test
